# Environment variables for the stack
# Copy this file to .env and update the values

# Domain configuration - replace with your actual domain
DOMAIN=localhost

# Traefik dashboard authentication (user:password in htpasswd format)
# Generate with: echo $(htpasswd -nB user) | sed -e s/\\$/\\$\\$/g
TRAEFIK_AUTH=admin:$$2y$$10$$yourhashhere

# Keycloak admin password
KEYCLOAK_ADMIN_PASSWORD=admin

# Keycloak client secret for demo app
KEYCLOAK_CLIENT_SECRET=your_client_secret