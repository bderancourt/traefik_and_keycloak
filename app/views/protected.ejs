<!DOCTYPE html>
<html>
<head>
    <title>Protected Page - Keycloak Demo</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 40px; }
        .container { max-width: 800px; margin: 0 auto; }
        .btn { padding: 10px 20px; margin: 10px; text-decoration: none; background: #007bff; color: white; border-radius: 5px; }
        .info { background: #d4edda; padding: 20px; border-radius: 5px; margin: 20px 0; border: 1px solid #c3e6cb; }
        .user-info { background: #f8f9fa; padding: 15px; border-radius: 5px; margin: 20px 0; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Protected Page</h1>
        
        <div class="info">
            <h3>🎉 Success!</h3>
            <p>You have successfully accessed this protected page. This page requires authentication through Keycloak.</p>
        </div>
        
        <div class="user-info">
            <h4>User Information:</h4>
            <p><strong>Username:</strong> <%= user.preferred_username %></p>
            <p><strong>Email:</strong> <%= user.email %></p>
            <p><strong>Realm:</strong> <%= user.iss.split('/').pop() %></p>
            <p><strong>Subject:</strong> <%= user.sub %></p>
        </div>
        
        <a href="/" class="btn">Back to Home</a>
        <a href="/logout" class="btn" style="background: #dc3545;">Logout</a>
    </div>
</body>
</html>